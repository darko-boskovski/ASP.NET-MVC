@model MenuViewModel
@{ ViewBag.Title = "Menu"; }

<h1>Pizza Menu</h1>
@for (int i = 0; i < Model.Menu.Count; i++)
{
    @Html.Partial("_PizzaItem", Model.Menu[i]);
}

<h1>Drinks Menu</h1>
@for (int i = 0; i < Model.DrinkMenu.Count; i++)
{
    @Html.Partial("_DrinkMenu", Model.DrinkMenu[i]);
}